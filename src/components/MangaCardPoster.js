import React, {useEffect, useState} from 'react';
import axios from 'axios';

const MangaCardPoster = (props) => {
  const [posterLarge, setPosterLarge] = useState()
  useEffect(async () => {
    
  })
}